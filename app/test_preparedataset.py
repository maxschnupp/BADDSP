import preparedataset

#just running it for now
#TODO: find out what valid pattern looks like for target folder
def test_preparedataset():
    folderPath = "testAudio"
    preparedataset.prepareDataset(folderPath)
